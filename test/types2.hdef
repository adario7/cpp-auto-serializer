`
using namespace std;
#include <string>
#include <vector>
#include <types1.hh>
`

// comment
/* these comment
 * will not be in the output header */
 `
 // unless inside backticks
 `

struct st2 {
	int a = `3`, b = `3`;
	st1 sub1;
	vector<st1> vec1;
`
	int getDiff(int j) {
		return a - b;
	}
`
};
